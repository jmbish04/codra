# Plain config (secrets come from the local `tokens` CLI, not here).
WORKER_URL=https://codra.hacolby.workers.dev
# Optional overrides:
# AGENT_ID=mac-studio
# POLL_MS=15000
# HEARTBEAT_MS=30000

# Secrets are pulled at runtime via:
#   tokens show WORKER_API_KEY --value-only
#   tokens show JULES_API_KEY --value-only
